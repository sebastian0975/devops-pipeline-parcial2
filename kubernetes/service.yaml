apiVersion: v1
kind: Service

metadata:
  name: microservicio-service
  namespace: devops

spec:
  selector:
    app: microservicio

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort